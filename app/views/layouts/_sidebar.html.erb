<!-- Sidebar -->
<ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar">

  <!-- Sidebar - Brand -->
  <%= link_to dashboard_path, class: "sidebar-brand d-flex align-items-center justify-content-center" do %>
    <div class="sidebar-brand-icon rotate-n-15">
      <i class="fas fa-laugh-wink"></i>
    </div>
    <div class="sidebar-brand-text mx-5"><%= fa_icon "terminal 4x" %></div>
  <% end %>

  <!-- Divider -->
  <hr class="sidebar-divider my-0">
 <% if account_signed_in? %>
  <li class="nav-item">
    <%= link_to(dashboard_path, :class => "nav-link") do %>
      <span>Feed</span>
    <% end %>
  </li>
  <hr class="sidebar-divider my-0">
  <li class="nav-item">
    <%= link_to(new_post_path, :class => "nav-link") do %>
      <span>Create Post</span>
    <% end %>
  </li>
  <hr class="sidebar-divider my-0">
  <li class="nav-item">
    <%= link_to(profile_path(current_account.username), :class => "nav-link", :method => :delete) do %>
      <span>My Profile</span>
    <% end %>
  </li>
  <li class="nav-item">
    <%= link_to(destroy_account_session_path, :class => "nav-link", :method => :delete) do %>
      <span>Sign Out</span>
    <% end %>
  </li>
<% end %>
</ul>
<!-- End of Sidebar -->
